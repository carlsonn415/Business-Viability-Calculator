version: 1
frontend:
  phases:
    preBuild:
      commands:
        - echo "Installing dependencies..."
        - npm ci
        - echo "Checking DATABASE_URL..."
        - echo "DATABASE_URL is ${DATABASE_URL:+set}" || echo "DATABASE_URL is NOT set"
        - echo "Generating Prisma Client..."
        - npx prisma generate
        - echo "Running database migrations..."
        - npx prisma migrate deploy
    build:
      commands:
        - echo "Building Next.js application..."
        - npm run build
  artifacts:
    baseDirectory: .next
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
      - .next/cache/**/*

